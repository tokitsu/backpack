<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="#">BACK PACKER's note</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          ユーザー
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <% if @current_user %>
            <%= link_to 'マイページ', user_path(@current_user), class: 'dropdown-item' %>
            <%= link_to 'ログアウト', logout_path, method: :delete, class: 'dropdown-item' %>
          <% else %>
            <%= link_to '登録', new_user_path, class: 'dropdown-item' %>
            <%= link_to 'ログイン', login_path, class: 'dropdown-item' %>
          <% end %>
        </div>
      </li>
      <% if @current_user %>
        <li class="nav-item">
          <%= header_link_item('マイページ', user_path(@current_user)) %>
        </li>
      <% else %>
      <% end %>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          情報ノート
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <%= link_to 'ノート一覧', boards_path , class: 'dropdown-item' %>
            <%= link_to '新規ノート作成', new_board_path, class: 'dropdown-item' %>
        </div>
      </li>

      <li class="nav-item">
        <%= header_link_item('旅仲間募集', root_path) %>
      </li>
      <li class="nav-item">
        <%= header_link_item('質問箱', root_path) %>
      </li>

    </ul>


  </div>
</nav>
