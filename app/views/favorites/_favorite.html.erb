<% favorite = @current_user.favorites.find_by(board_id: board.id) %>
<% if favorite.present? %>
  <%= link_to 'お気に入り解除',board_add_path(board), method: :delete, id: "favorite-button", remote: true %>
<% else %>
  <%= link_to 'お気に入り', board_add_path(board), method: :post, id: "favorite-button", remote:true %>
<% end %>
