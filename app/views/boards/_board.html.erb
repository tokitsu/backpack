<ul class="list-group">
  <li class="list-group-item">
    <div class="board_user">
      <% if @board.user.image.attached? %>
        <%=link_to image_tag(@board.user.image.variant(auto_orient: true),width:45), board.user %>
      <% else %>
        <%= link_to image_tag('human.jpeg', width: 40), board.user %>
      <% end %>
    <div class= 'board_name'><%= @board.user.name %></div>
    </div>
    <div class="board_title">
      <%= @board.title %><br>
    <p class="board_update">
      <%= @board.updated_at.to_s(:datetime_jp)%>　　
      <%= @board.country%>　<%= @board.city%>
    </p>
    </div>
    <div class="board_tag">
      <% @board.tags.each do |tag| %>
        <span class ="badge badge-success"><%= tag.name %></span>
      <% end %>
    </div>
  </li>
  <li class="list-group-item">
    <div class="board_body">
      <p class="card-text"><%= simple_format(@board.body) %></p>
      <p class="card-left">
        <% if @board.images.attached? %>
          <% @board.images.each do |image| %>
            <%= image_tag image.variant(auto_orient: true),height:200 %>
          <% end %>
        <% end %>
      </p>
    </div>
  </li>
  <li class="list-group-item">
    <% if Favorite.exists?(user_id:@current_user.id, board_id:board.id)%>
      <%= link_to 'お気に入り解除',board_add_path(board), method: :delete %>
    <% else %>
      <%= link_to 'お気に入り', board_add_path(board), method: :post %>
    <% end %>
  </li>
</ul><br>
